# v1.1 · 让记账本导出「报表.txt」

> **类型**：**加功能版**（在你那版活的 `ledger.py` 上追加，不改原来的逻辑）
> **建议用时**：30~60 分钟 ｜ **交付物**：改好的 `ledger.py` ＋ 跑出来的 `报表.txt`
> **这一版你多会了什么**：程序不只在**屏幕**上说话，还能**留下一份文件**给别人看。
> 对外承诺兑现：README 的「后续计划」里写着 **v1.1 导出报表到 `报表.txt`** —— 做完就能划掉一项。

---

## 一、要做什么（一句话）

运行记账本之后，除了屏幕上那几行，**目录里还要多出一个 `报表.txt`**，内容是"按类别"那张表的**文件版**。

## 二、输出格式（按你**当前账本**的 13 笔，逐字给出）

**屏幕**（和 v1.0 **完全一样**，一个字都不许变）：
```
1. 苹果 5 未分类
...（共 13 行）
共13笔 总额1102378 平均84798.30769230769
--- 按类别 ---
未分类: 1000213 (6 笔)
吃饭: 21 (1 笔)
生活: 91 (1 笔)
水果: 53 (3 笔)
装饰: 100000 (1 笔)
物品: 2000 (1 笔)
```

**`报表.txt`（新做的）** —— 就是"按类别"那 **6 行**，**格式逐字一致**，**不含** `--- 按类别 ---` 这行标题：
```
未分类: 1000213 (6 笔)
吃饭: 21 (1 笔)
生活: 91 (1 笔)
水果: 53 (3 笔)
装饰: 100000 (1 笔)
物品: 2000 (1 笔)
```

## 三、加在哪（照做）

打开 `money-ledger\ledger.py` → 文件**最下面**有一段"把数据写回 `money.json`"的代码 → **在那段的前面**插入新的"导出报表"代码。

- ✅ **只加，不改**：原来的读账本 / 收账 / 统计 / 打印 / 写回**一行都不动**
- ✅ 报表要"**每次运行都重写**"（不要追加，否则连跑两次会变 12 行）
- ⚠️ **先备份**：`copy money.json money.bak.json`（程序每次都会写回它）

## 四、约束（老规矩）

1. **只改 `ledger.py`，逻辑一行都别动**
2. **不给格式、不给代码** —— `f.write` ＋ `"w"` 你已经用过 3 次（R2 成绩报表 → R3 开销报表 → R4 调用报表）→ **这次自己默出来**
3. 文件必须生成在 **`money-ledger\` 目录里**
4. 想不起写法：**一次只准看一行**你自己写过的 `review3\开销统计.py`（或 R2/R4）；**不许整段复制** —— 复制一次这版就白做
5. 卡住 → 喊「**一级提示**」（我只说方向，不给代码）

## 五、验收清单（做完自己打勾，再交我）

- [ ] Run 一次后，目录里**出现 `报表.txt`**
- [ ] `报表.txt` **6 行**，与上面那段**逐字一致**（类别名、冒号、空格、`(N 笔)`）
- [ ] **连跑两次** → 报表**还是 6 行**（验证你用的是覆盖写，不是追加）
- [ ] **屏幕输出与 v1.0 完全一样**（没把原来的打印弄坏）
- [ ] **删掉 `报表.txt`** 再跑一次 → 自动重建
- [ ] `money.json` 没被弄坏（跑完再开一次程序，旧账还在）

## 六、做完之后

1. 打卡 **`【v1.1 完成】`** ＋ 屏幕输出 ＋ `报表.txt` 内容
2. 提交（**你敲**，我只给命令）：`git add .` → `git commit -m "v1.1 导出报表"` → `git push`
3. 下一个：**v1.2 按类别排序**（还是这个仓库）

> 备注（老师已定，不用你操心）：`报表.txt` **要提交进仓库** —— 它是"别人照着跑应该看到的东西"，正好和 README 的样例互相印证。
