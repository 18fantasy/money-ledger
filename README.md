# 记账本 · money-ledger

> 一个跑在终端里的小记账程序：**记一笔 → 存进文件 → 下次打开还在 → 按类别看"钱花在哪了"**。

## 为什么写它
（**换成你自己的话**，1~2 句。示例：手机记账 App 要注册、要联网、数据在别人服务器上；我只想要一个**离线、数据在自己电脑上**的最小工具，顺便把它做成我的第一个完整项目。）

## 功能
- [x] 记一笔：`物品 金额 类别`（空格分隔，输 `q` 结束）
- [x] **数据持久化**：存进 `money.json`，关掉程序再打开旧账还在
- [x] 清单 + 汇总：笔数 / 总额 / 平均
- [x] **按类别汇总**：每一类花了多少、几笔
- [ ] 导出报表到 `报表.txt`（v1.1 计划）
- [ ] 按类别排序（v1.2 计划）

## 快速开始（3 步）
```bash
git clone https://github.com/<你的用户名>/money-ledger.git
cd money-ledger
python ledger.py
```
> 需要 **Python 3.8+**；**不需要安装任何第三方库**（只用了标准库 `json`）。

## 样例

输入（每行一笔，输 `q` 结束）：
```
苹果 18 水果
q
```

输出（用仓库自带的 11 条演示数据）：
```
1. 苹果 5 未分类
2. 手表 100 未分类
3. 苹果 5 未分类
4. 手表 100 未分类
5. 汽车 1000000 未分类
6. 可乐 3 未分类
7. 大米 21 吃饭
8. 被子 91 生活
9. 苹果 18 水果
10. 苹果 15 水果
11. 手表 100000 装饰
共11笔 总额1100358 平均100032.54545454546
--- 按类别 ---
未分类: 1000213 (6 笔)
吃饭: 21 (1 笔)
生活: 91 (1 笔)
水果: 33 (2 笔)
装饰: 100000 (1 笔)
```

## 项目结构
```
money-ledger/
├── ledger.py       # 全部逻辑：读旧账 / 收新账 / 两个统计函数 / 打印 / 写回
├── money.json      # 账本数据（演示用；删掉它就从零开始记）
├── .gitignore      # 不上传缓存和虚拟环境
└── README.md
```

## 用到的技术（说人话 + 对应岗位关键词）
| 技术 | 在哪 | 干什么 |
|---|---|---|
| **文件持久化** | `json.load` / `json.dump` | 数据落在磁盘上，程序重启还在（对应 JD 里的"文件/数据处理"） |
| **列表 + 字典** | `records` | 一个列表装字典：`[{"item":…, "money":…, "category":…}]` |
| **字典当计数器** | `result[键] = result.get(键, 0) + 值` | 按类别汇总（读旧值 → 加 → 写回） |
| **函数 + `return`** | `show()` / `sum_by_category()` / `count_by_category()` | 把逻辑拆开，各自交回结果 |
| **兼容旧数据** | `.get("category", "未分类")` | 早期数据没有 `category` 字段也不会崩 |

## 量化指标
- 演示数据：**11 笔 / 总额 1100358 / 5 个类别**
- 代码：**1 个文件、约 58 行、0 个第三方依赖**
- 启动到出结果：**< 0.1 秒**

## 后续计划
- **v1.1** 导出报表到 `报表.txt`
- **v1.2** 按类别排序、按金额筛选
- **v2.0** 换成 SQLite 数据库 + 命令行参数（`--month 2026-09`）

## 关于
我在学 **Python + AI 应用开发** 方向，这是我第一个完整的小项目。技术博客：（放你的博客链接，之后补）
